package com.example.gamedock.ui

import androidx.compose.ui.unit.dp

object Dimens {
    // Shared spacing constants for padding/margins.
    val screenPadding = 16.dp
    val cardSpacing = 12.dp
}

object Strings {
    // Shared strings used across multiple surfaces.
    const val appName = "GameDock"
    const val freebiesTitle = "Free Games Available Now"
}
