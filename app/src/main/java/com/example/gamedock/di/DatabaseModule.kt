package com.example.gamedock.di

import com.example.gamedock.data.local.AppDatabase

/**
 * Creates database related dependencies (placeholder for Room).
 */
object DatabaseModule {
    fun provideDatabase(): AppDatabase? = null
}
